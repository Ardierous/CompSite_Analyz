# Как передается API ключ через .env файл

## 🔄 Процесс передачи переменных окружения

### Шаг 1: Создание .env файла на VPS

`.env` файл создается **на VPS (на хосте)**, не внутри контейнера:

```
~/comp-site-analyz/
├── .env                    ← Файл создается здесь (на хосте)
└── scripts/
    └── docker-compose.prod.yml
```

**Содержимое `.env`:**
```env
OPENAI_API_KEY=sk-ваш-реальный-ключ-здесь
OPENAI_API_BASE=https://api.proxyapi.ru/openai/v1
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
FLASK_DEBUG=False
```

### Шаг 2: Docker Compose читает .env файл

В `docker-compose.prod.yml` есть строка:

```yaml
env_file:
  - ../.env    ← Docker Compose читает переменные из этого файла
```

**Что происходит:**
- Docker Compose находит файл `.env` в директории `~/comp-site-analyz/.env`
- Читает все переменные из файла
- Автоматически передает их в контейнер как переменные окружения

### Шаг 3: Переменные попадают в контейнер

Когда контейнер запускается, все переменные из `.env` автоматически становятся доступны внутри контейнера:

```bash
# Внутри контейнера эти переменные доступны как:
OPENAI_API_KEY=sk-ваш-реальный-ключ-здесь
OPENAI_API_BASE=https://api.proxyapi.ru/openai/v1
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
FLASK_DEBUG=False
```

### Шаг 4: Приложение читает переменные

Внутри контейнера приложение (`main.py`, `Agents_crew.py`) читает переменные через:

```python
import os
from dotenv import load_dotenv

# Загружает переменные из окружения (которые передал Docker Compose)
load_dotenv()  # Это читает переменные, которые уже есть в окружении

# Или напрямую:
api_key = os.getenv("OPENAI_API_KEY")  # Получает значение из окружения
```

## 📋 Визуализация процесса

```
┌─────────────────────────────────────────────────────────┐
│ VPS (хост)                                               │
│                                                          │
│  ~/comp-site-analyz/                                     │
│  ├── .env  ← Содержит: OPENAI_API_KEY=sk-xxx...         │
│  └── scripts/                                            │
│      └── docker-compose.prod.yml                        │
│          └── env_file: - ../.env  ← Указывает на .env   │
│                                                          │
│  docker-compose up -d                                    │
│       ↓                                                  │
│  Читает .env файл                                        │
│  Передает переменные в контейнер                        │
└─────────────────────────────────────────────────────────┘
                        ↓
┌─────────────────────────────────────────────────────────┐
│ Docker Контейнер                                          │
│                                                          │
│  Переменные окружения:                                   │
│  - OPENAI_API_KEY=sk-xxx...  ← Передано из .env         │
│  - OPENAI_API_BASE=https://...                          │
│  - FLASK_HOST=0.0.0.0                                    │
│  - FLASK_PORT=5000                                       │
│                                                          │
│  Приложение (main.py):                                   │
│  os.getenv("OPENAI_API_KEY")  ← Читает из окружения    │
└─────────────────────────────────────────────────────────┘
```

## ✅ Важные моменты

1. **`.env` файл НЕ попадает в Docker образ**
   - `.env` находится только на хосте (VPS)
   - Он не копируется в образ при сборке
   - Это безопасно - ключи не хранятся в образе

2. **Docker Compose автоматически передает переменные**
   - Не нужно указывать каждую переменную вручную
   - Все переменные из `.env` автоматически доступны в контейнере

3. **Путь к .env файлу**
   - В `docker-compose.prod.yml`: `env_file: - ../.env`
   - `../` означает "на уровень выше" от `scripts/`
   - То есть `.env` должен быть в `~/comp-site-analyz/.env`

## 🔍 Проверка передачи переменных

Чтобы убедиться, что переменные переданы правильно:

```bash
# Проверка переменных внутри контейнера
docker-compose -f scripts/docker-compose.prod.yml exec web env | grep OPENAI

# Должно показать:
# OPENAI_API_KEY=sk-ваш-ключ
# OPENAI_API_BASE=https://api.proxyapi.ru/openai/v1
```

## 🚨 Частые ошибки

### Ошибка 1: .env файл не найден

```
ERROR: .env file not found
```

**Решение:** Убедитесь, что `.env` находится в `~/comp-site-analyz/.env`

### Ошибка 2: Переменные не передаются

**Причина:** Неправильный путь в `env_file`

**Решение:** Проверьте путь в `docker-compose.prod.yml`:
```yaml
env_file:
  - ../.env  # Правильно: на уровень выше от scripts/
```

### Ошибка 3: API ключ не работает

**Причина:** В `.env` указан неправильный ключ или ключ не сохранен

**Решение:** 
```bash
# Проверьте содержимое .env
cat ~/comp-site-analyz/.env

# Убедитесь, что ключ указан правильно (без пробелов, кавычек)
```

## 📝 Пример полного процесса

```bash
# 1. Создаем .env на VPS
cd ~/comp-site-analyz
cat > .env << 'EOF'
OPENAI_API_KEY=sk-ваш-реальный-ключ-здесь
OPENAI_API_BASE=https://api.proxyapi.ru/openai/v1
FLASK_HOST=0.0.0.0
FLASK_PORT=5000
FLASK_DEBUG=False
EOF

# 2. Запускаем контейнер
docker-compose -f scripts/docker-compose.prod.yml up -d

# 3. Проверяем, что переменные переданы
docker-compose -f scripts/docker-compose.prod.yml exec web env | grep OPENAI

# 4. Проверяем логи приложения
docker-compose -f scripts/docker-compose.prod.yml logs | grep -i "api key"
```

## 🎯 Итого

1. **Создайте `.env` на VPS** в `~/comp-site-analyz/.env`
2. **Укажите свой API ключ** в файле `.env`
3. **Docker Compose автоматически передаст** все переменные в контейнер
4. **Приложение прочитает** переменные из окружения

**Важно:** `.env` файл должен быть на хосте (VPS), а не внутри контейнера!

