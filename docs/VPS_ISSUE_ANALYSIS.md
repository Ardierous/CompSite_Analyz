# Анализ проблемы с доступностью на VPS

**Дата:** 2025-12-27  
**VPS IP:** 45.133.245.186  
**Проблема:** Страница недоступна по адресу http://45.133.245.186:5000/

## 📋 Анализ лога от агента на VPS

### Что было сделано:

1. ✅ Контейнер запущен: `avardous/comp_site_analyz:20251227-224241`
2. ✅ Порт 5000 проброшен на хост
3. ✅ Контейнер работает и доступен локально: `http://localhost:5000`
4. ❌ **Проблема:** Страница недоступна извне по `http://45.133.245.186:5000/`

### Потенциальные проблемы:

#### 1. ⚠️ Контейнер запущен через `docker run` вместо `docker-compose`

**В логе:**
```
📝 Команда для запуска образа:
   docker run -p 5000:5000 --env-file .env avardous/comp_site_analyz:20251227-224241
```

**Проблема:**
- Контейнер запущен через `docker run`, а не через `docker-compose`
- Это может работать, но не рекомендуется для продакшена
- Переменные окружения могут не применяться правильно

**Рекомендация:** Использовать `docker-compose` для запуска:

```bash
cd ~/comp-site-analyz
docker-compose -f scripts/docker-compose.prod.yml up -d
```

#### 2. ⚠️ Файл `.env` не найден

**В логе:**
```
**Примечание:** файл `.env` не найден, контейнер запущен без него.
```

**Проблема:**
- Без `.env` файла могут отсутствовать важные переменные окружения
- `OPENAI_API_KEY` и другие настройки не загружены
- Приложение может не работать корректно

**Решение:**
```bash
# Создать .env файл из env.example
cp env.example .env
nano .env  # Заполнить значениями
```

#### 3. ❌ **ГЛАВНАЯ ПРОБЛЕМА: Файрвол не настроен**

**Симптом:** Приложение работает локально, но недоступно извне

**Причина:** Ubuntu 20.04 использует UFW (Uncomplicated Firewall), который по умолчанию блокирует все входящие подключения, кроме SSH.

**Решение:**
```bash
# Проверить статус файрвола
sudo ufw status

# Открыть порт 5000
sudo ufw allow 5000/tcp

# Если файрвол не активен, активировать его
sudo ufw enable

# Проверить, что порт открыт
sudo ufw status | grep 5000
```

**Важно:** Перед включением файрвола убедитесь, что SSH порт открыт:
```bash
sudo ufw allow 22/tcp
sudo ufw enable
```

#### 4. ⚠️ Проверка проброса порта

**Проверка:**
```bash
# Проверить, что порт проброшен
docker ps | grep comp_site_analyz

# Должно быть: 0.0.0.0:5000->5000/tcp

# Проверить, что приложение слушает на 0.0.0.0
docker exec comp_site_analyz netstat -tlnp | grep :5000

# Должно быть: 0.0.0.0:5000
```

## ✅ Правильная последовательность запуска на VPS

### Шаг 1: Подготовка файлов

```bash
# Создать директорию проекта
mkdir -p ~/comp-site-analyz
cd ~/comp-site-analyz

# Создать структуру
mkdir -p scripts

# Скопировать файлы:
# - scripts/docker-compose.prod.yml → ~/comp-site-analyz/scripts/docker-compose.prod.yml
# - env.example → ~/comp-site-analyz/.env
```

### Шаг 2: Настройка .env

```bash
# Создать .env из примера
cp env.example .env

# Отредактировать .env
nano .env

# Минимально необходимые переменные:
# OPENAI_API_KEY=your_key_here
# OPENAI_API_BASE=https://api.proxyapi.ru/openai/v1
# FLASK_HOST=0.0.0.0
# FLASK_PORT=5000
# FLASK_DEBUG=False
```

### Шаг 3: Настройка файрвола

```bash
# Открыть SSH порт (если файрвол не настроен)
sudo ufw allow 22/tcp

# Открыть порт 5000
sudo ufw allow 5000/tcp

# Активировать файрвол (если не активен)
sudo ufw enable

# Проверить статус
sudo ufw status
```

### Шаг 4: Запуск контейнера

```bash
# Перейти в корень проекта
cd ~/comp-site-analyz

# Остановить предыдущий контейнер (если есть)
docker-compose -f scripts/docker-compose.prod.yml down

# Запустить контейнер
docker-compose -f scripts/docker-compose.prod.yml up -d

# Проверить статус
docker-compose -f scripts/docker-compose.prod.yml ps

# Просмотреть логи
docker-compose -f scripts/docker-compose.prod.yml logs -f
```

### Шаг 5: Проверка доступности

```bash
# Проверка локально
curl http://localhost:5000

# Проверка извне (с другого компьютера)
curl http://45.133.245.186:5000
```

## 🔍 Диагностика проблем

### Проверка 1: Статус контейнера

```bash
docker ps | grep comp_site_analyz
```

**Ожидаемый результат:**
```
CONTAINER ID   IMAGE                                          STATUS
xxx            avardous/comp_site_analyz:latest               Up X minutes
PORTS
0.0.0.0:5000->5000/tcp
```

### Проверка 2: Порт на хосте

```bash
sudo netstat -tlnp | grep :5000
```

**Ожидаемый результат:**
```
tcp        0      0 0.0.0.0:5000            0.0.0.0:*               LISTEN      xxxx/docker-proxy
```

### Проверка 3: Приложение внутри контейнера

```bash
docker exec comp-site-analyz netstat -tlnp | grep :5000
```

**Ожидаемый результат:**
```
tcp        0      0 0.0.0.0:5000            0.0.0.0:*               LISTEN      xxxx/python
```

### Проверка 4: Файрвол

```bash
sudo ufw status | grep 5000
```

**Ожидаемый результат:**
```
5000/tcp                     ALLOW       Anywhere
```

### Проверка 5: Логи контейнера

```bash
docker-compose -f scripts/docker-compose.prod.yml logs --tail=50
```

**Ожидаемый результат:**
```
Запуск Flask приложения...
Сервер будет доступен по адресу: http://0.0.0.0:5000
```

## ✅ Вывод

**Основная проблема:** Файрвол UFW не настроен для открытия порта 5000.

**Решение:**
1. Открыть порт 5000 в файрволе: `sudo ufw allow 5000/tcp`
2. Использовать `docker-compose` вместо `docker run` для запуска
3. Создать `.env` файл с необходимыми переменными окружения

**После настройки файрвола приложение будет доступно по адресу:**
- `http://45.133.245.186:5000/`


