# Инструкция по установке и диагностике на VPS

> 📌 **Быстрая справка:** См. `VPS_QUICK_REFERENCE.md`  
> 🔑 **Передача API ключа:** См. `VPS_ENV_SETUP.md` (раздел 1)  
> ✅ **Проверка CrewAI:** См. `VPS_ENV_SETUP.md` (раздел 2)

## Проблема: "CrewAI не доступен. Проверьте установку зависимостей"

### Шаг 1: Подключение к VPS

Используйте SSH для подключения к вашему VPS:
```bash
ssh Service
# или
ssh root@95.81.123.146
```

### Шаг 2: Проверка версии Python

CrewAI требует Python 3.9-3.11:
```bash
python3 --version
# или
python --version
```

Если версия не подходит, установите Python 3.11:
```bash
# Ubuntu 20.04
sudo apt update
sudo apt install python3.11 python3.11-venv python3.11-dev
```

### Шаг 3: Переход в директорию проекта

```bash
cd /path/to/your/project
# или если проект в домашней директории
cd ~/Company
```

### Шаг 4: Создание виртуального окружения (если еще не создано)

```bash
python3.11 -m venv venv
source venv/bin/activate
```

### Шаг 5: Установка зависимостей

```bash
# Обновляем pip
pip install --upgrade pip

# Устанавливаем зависимости
pip install -r requirements.txt

# Если есть проблемы, устанавливаем CrewAI отдельно
pip install 'crewai[tools]>=0.11.2'
```

### Шаг 6: Проверка установки

> 📖 **Подробная инструкция:** См. `VPS_ENV_SETUP.md` (раздел 2)

**Быстрая проверка:**
```bash
python3 -c "import crewai; print(f'CrewAI: {crewai.__version__}')"
```

**Полная диагностика:**
Запустите скрипт диагностики:
```bash
python check_crewai.py
```

Этот скрипт проверит:
- Версию Python
- Установку CrewAI
- Доступность модулей
- Переменные окружения
- Импорт Agents_crew

**Если скрипт не на VPS, скопируйте его:**
```powershell
# На локальном компьютере (Windows)
scp check_crewai.py Service:~/Company/
```

### Шаг 7: Проверка переменных окружения

> 📖 **Подробная инструкция:** См. `VPS_ENV_SETUP.md` (раздел 1)

Убедитесь, что файл `.env` содержит необходимые переменные:
```bash
cat .env
```

Должны быть установлены:
- `OPENAI_API_KEY` - ваш API ключ OpenAI
- `OPENAI_API_BASE` (опционально) - базовый URL API

**Как создать .env на VPS:**
```bash
nano .env
# Добавьте:
# OPENAI_API_KEY=sk-ваш-ключ
# OPENAI_API_BASE=https://api.proxyapi.ru/openai/v1
chmod 600 .env
```

### Шаг 8: Запуск приложения

```bash
python main.py
```

Если ошибка сохраняется, проверьте логи в консоли.

## Решение проблем

### Проблема 1: CrewAI не установлен

**Симптомы:**
```
ImportError: No module named 'crewai'
```

**Решение:**
```bash
pip install 'crewai[tools]>=0.11.2'
```

### Проблема 2: Несовместимая версия Python

**Симптомы:**
```
ERROR: Package 'crewai' requires a different Python: 3.12.0 not in '>=3.9,<3.12'
```

**Решение:**
Используйте Python 3.11:
```bash
python3.11 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
```

### Проблема 3: Ошибка при создании агентов

**Симптомы:**
```
ОШИБКА при создании Web Scraper Agent: ...
```

**Решение:**
1. Проверьте переменные окружения (OPENAI_API_KEY)
2. Проверьте логи для детальной информации
3. Убедитесь, что все зависимости установлены

### Проблема 4: NumPy версия несовместима

**Симптомы:**
```
ERROR: numpy>=2.0 is not supported
```

**Решение:**
```bash
pip install --force-reinstall "numpy<2.0"
```

## Использование Docker (альтернативный способ)

Если проблемы с установкой зависимостей, используйте Docker:

```bash
# Сборка образа
docker build -t company-analyzer .

# Запуск контейнера
docker run -d \
  -p 5000:5000 \
  -e OPENAI_API_KEY=your_key_here \
  -e OPENAI_API_BASE=your_base_url \
  --name company-app \
  company-analyzer
```

## Проверка работы

После запуска приложения проверьте:
```bash
curl http://localhost:5000/
```

Должна открыться веб-страница приложения.

## Логи и отладка

Проверьте логи приложения:
```bash
# Если запущено в консоли, логи будут видны
# Если запущено как сервис, проверьте:
journalctl -u your-service-name
# или
tail -f /var/log/your-app.log
```

## Контакты для помощи

Если проблема не решена:
1. Запустите `python check_crewai.py` и сохраните вывод
2. Проверьте логи приложения
3. Убедитесь, что все зависимости установлены правильно

