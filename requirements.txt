# Web application dependencies
# Для кнопки «MD → DOCX» (Pandoc) требуется установленный Pandoc: https://pandoc.org/installing.html
flask>=3.0.0
flask-cors>=4.0.0
python-dotenv>=1.0.0
requests>=2.31.0
beautifulsoup4>=4.12.0
python-docx>=1.1.0

# NumPy - фиксируем версию <2.0 для совместимости с архитектурой
numpy<2.0

# CrewAI and AI dependencies
# IMPORTANT: CrewAI requires Python 3.10+ (3.11 recommended)
# Устанавливаем crewai с инструментами (tools extras включает все необходимые зависимости)
# Обновлено до 0.80+ для совместимости с pydantic и исправления TypeError | None
crewai[tools]>=0.80.0
pydantic>=2.10.0,<3
openai>=1.7.1,<2.0.0
