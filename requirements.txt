# Web application dependencies
flask>=3.0.0
flask-cors>=4.0.0
python-dotenv>=1.0.0
requests>=2.31.0
python-docx>=1.1.0

# NumPy - фиксируем версию <2.0 для совместимости с архитектурой
numpy<2.0

# CrewAI and AI dependencies
# IMPORTANT: CrewAI requires Python 3.9-3.11
# Recommended: Use Python 3.11 (stable, widely supported)
# Устанавливаем crewai с инструментами (tools extras включает все необходимые зависимости)
# Поддерживаются версии от 0.11.2 и выше (включая 1.7.2+)
crewai[tools]>=0.11.2
openai>=1.7.1,<2.0.0
